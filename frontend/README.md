# Frontend

Frontend application work starts after the Phase 1 backend/database foundation.
The existing static prototype remains at the repository root and is not part of
the authenticated Phase 1 Compose stack.

